### Form

- By using the form, let a user input on the corresponding input fields and make use of the `div#output` to show the values inputted in the input fields inside the form.

- Do the DOM manipulation inside `script.js`.
